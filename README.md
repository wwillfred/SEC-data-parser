# SEC data parser
***WIP***

## Prerequisites
Install json-c with [homebrew][json-c_homebrew]:

`brew install json-c`

download JSON files from SEC.gov and place in `sec-data` directory
**need to include code***

## Compiling
Use the `ljson-c` flag when compiling with `gcc`.

[json-c_homebrew]: https://formulae.brew.sh/formula/json-c
